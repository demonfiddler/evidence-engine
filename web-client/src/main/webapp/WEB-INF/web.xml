<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>Evidence Engine Web Client</display-name>
    <description>
        Static Next.js client application for the Evidence Engine platform.
    </description>

    <!-- Build-time profile injected by Gradle -->
    <context-param>
        <param-name>cacheProfile</param-name>
        <param-value>${cacheProfile}</param-value>
    </context-param>

    <!-- Cache control filter -->
    <filter>
        <filter-name>CacheControlFilter</filter-name>
        <filter-class>io.github.demonfiddler.ee.webclient.filter.CacheControlFilter</filter-class>
    </filter>

    <!-- Apply filter to HTML pages -->
    <filter-mapping>
        <filter-name>CacheControlFilter</filter-name>
        <url-pattern>*.html</url-pattern>
    </filter-mapping>

</web-app>
