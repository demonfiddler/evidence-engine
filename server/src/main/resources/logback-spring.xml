<configuration>
    <!-- CoPilot suggests this as a fallback default. -->
    <!-- include resource="org/springframework/boot/logging/logback/defaults.xml" /-->

    <!-- ================================= -->
    <!-- DEVELOPMENT MODE (console output) -->
    <!-- ================================= -->
    <springProfile name="!production">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="io.github.demonfiddler.ee.server" level="INFO"/>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>


    <!-- ============================== -->
    <!-- PRODUCTION (Tomcat file output) -->
    <!-- ============================== -->
    <springProfile name="production">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>/var/log/tomcat11/ee-server.log</file>

            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>/var/log/tomcat11/ee-server.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>14</maxHistory>
            </rollingPolicy>

            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="io.github.demonfiddler.ee.server" level="INFO"/>

        <root level="INFO">
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

</configuration>